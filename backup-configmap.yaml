apiVersion: v1
data:
  main-snippet: load_module /opt/dynatrace/oneagent/agent/bin/current/linux-musl-x86-64/liboneagentnginx.so;
  nginx.conf: |
    user nginx;
    worker_processes auto;
    pid /run/nginx.pid;
    events {
     worker_connections 1024;
    }
    http {
     sendfile on;
     tcp_nopush on;
     tcp_nodelay on;
     keepalive_timeout 65;
     types_hash_max_size 2048;
     access_log off;
     include /etc/nginx/mime.types;
     default_type application/octet-stream;
     access_log /var/log/nginx/access.log;

     upstream frontend {
     server frontend:80;
     }
     server {
     listen 80;
     server_name localhost;
     location / {
     proxy_pass http://frontend;
     }
     }
    }
kind: ConfigMap
metadata:
  creationTimestamp: "2022-02-04T19:34:49Z"
  name: nginx-conf
  namespace: default
  resourceVersion: "17116703"
  uid: 0856f0e4-a84c-49bd-8dd0-f5336eb65517
